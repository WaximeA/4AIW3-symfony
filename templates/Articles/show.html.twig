{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-md-12">
                <h1>L'article n°{{ article.id }}:</h1>
                <h3>{{ article.name }}</h3>
                <p>{{ article.content }}</p>
            </div>
        </div>
        {% include 'Page/back.html.twig' with {'backLink': 'app_article_index'} %}
    </div>
{% endblock %}

